stages:
    - compile
    - test

variables:
    GIT_SUBMODULE_STRATEGY: recursive
    GIT_STRATEGY: fetch


.ciconf:
    only:
        variables:
            - $CI_PROJECT_NAMESPACE == "keszocze"

.try_compiling:
    extends: .ciconf
    stage: compile
    artifacts:
        paths:
            - build
        expire_in: 30 minutes

    script:
        - mkdir -p build/bw_output
        - cd build
        - cmake ..
        - /opt/sonar-build-wrapper/build-wrapper-linux-x86-64 --out-dir bw_output make -j 4 all


.run_sonarqube:
    extends: .ciconf
    stage: test # we have to wait for new data to be present in bw_output
    script: 
        - /opt/sonar-scanner/bin/sonar-scanner

run_tests:
    extends: .ciconf
    stage: test
    script:
        - echo "We should automatically run tests"
        #- cd build/tests
        #  # the leading dot seems to be necessary
        #- ./error_metrics_test
        #- ./operations_test
        #- ./parsing_test
